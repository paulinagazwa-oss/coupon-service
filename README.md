# coupon-service
A lightweight REST microservice for managing discount coupons. The service exposes two endpoints: Create a coupon — allows adding a new discount coupon to the system Register coupon usage — allows a user to register the use of an existing coupon
